from setuptools import setup, find_packages

setup(
    name="pymath",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Dependencies here
    ],
)

''' py setup.py sdist bdist '''